# Compose env-file override used by `make k6-flash-sale-postgres`.
# Routes ReserveStock through Postgres SELECT ... FOR UPDATE instead of
# the Redis Lua atomic path. Always combined with env.no-ratelimit.
USE_POSTGRES_STOCK=true

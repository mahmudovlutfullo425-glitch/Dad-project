# Compose env-file override used by `make k6-product-detail-baseline`.
# Layered on top of .env via:
#   docker compose --env-file .env --env-file scripts/loadtest/env.cache-off ...
# Effect: api boots with the product hot-cache code path disabled.
PRODUCT_CACHE_ENABLED=false

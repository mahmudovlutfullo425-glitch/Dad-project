# Compose env-file override used by `make k6-flash-sale-*`.
# Bypasses the token-bucket rate limiter on both api and inventory so
# the load test measures the stock backend, not bucket rejections.
RATE_LIMIT_ENABLED=false
